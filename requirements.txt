# Web framework
flask>=2.3,<3.0

# Data science
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

# Feature extraction
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9          # faster XML/HTML parser (used by bs4)
python-whois>=0.8
python-dateutil>=2.8
googlesearch-python>=1.2

# Real-time WebSocket progress
flask-socketio>=5.3

# Explainability (SHAP values)
shap>=0.43

# WSGI server for production (optional but recommended)
gunicorn>=21.0; sys_platform != "win32"
waitress>=3.0; sys_platform == "win32"
